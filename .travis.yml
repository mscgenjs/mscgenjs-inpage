sudo: required

language: node_js

node_js:
  - stable

install:
  - npm install
  - npm run build

script:
  - npm run check

branches:
  except:
    - gh-pages

cache:
  directories:
    - node_modules

addons:
   code_climate:
     repo_token: $CODECLIMATE_REPO_TOKEN
